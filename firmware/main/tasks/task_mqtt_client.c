#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_log.h"

static const char *TAG = "MQTT_CLIENT";

void task_mqtt_client_start(void) {
    ESP_LOGI(TAG, "MQTT client task placeholder");
}
