#include "freertos/FreeRTOS.h"
#include "freertos/task.h"
#include "esp_log.h"

static const char *TAG = "SYNC_MGR";

void task_sync_manager_start(void) {
    ESP_LOGI(TAG, "Sync manager task placeholder");
}
